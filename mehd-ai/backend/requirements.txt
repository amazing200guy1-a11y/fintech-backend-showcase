# Mehd AI — Pinned Dependencies (Security Hardened)
# NEVER use unpinned versions in production.
# Run: pip install -r requirements.txt
# Audit: python -m pip_audit -r requirements.txt (run before every deploy)
# Last CVE scan: 2026-08-02 — 14 CVEs patched (cryptography x4, starlette x8 via fastapi upgrade, python-dotenv x1)

fastapi==0.138.0
uvicorn==0.34.0
httpx==0.28.1
pydantic==2.10.4
slowapi==0.1.9
python-dotenv==1.2.2
sse-starlette==2.2.1
firebase-admin==6.6.0
stripe==11.4.1
sentry-sdk[fastapi]==2.19.2
cryptography==48.0.1
cachetools==5.5.0
redis[asyncio]==5.2.1
# starlette resolved transitively via fastapi==0.138.0 (ships with starlette>=1.3.1, fixing 8 CVEs)
